package com.example.proyecto_final_javig.ui.theme

import androidx.compose.runtime.mutableStateOf

var colorCarta = mutableStateOf(carta_default)
var colorFondo = mutableStateOf(fondo_default)
var colorBoton = mutableStateOf(boton_default)
var colorTexto = mutableStateOf(texto_default)
var colorListaPrincipal = mutableStateOf(listas_principales_default)
var colorBorde = mutableStateOf(borde_default)
var colorAlpha1 = mutableStateOf(alpha_default1)
var colorAlpha2 = mutableStateOf(alpha_default2)


